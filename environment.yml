# Miniforge environment for standalone package development. pyproject.toml is
# authoritative for runtime and development dependency ranges.
#
#   conda env create -f environment.yml
#   conda activate imvpy
name: imvpy
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - -e .[dev]
